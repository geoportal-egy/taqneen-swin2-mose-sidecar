fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
numpy==1.26.4
rasterio==1.3.10
Pillow==10.4.0
PyYAML==6.0.2
# torch 2.7 is the first stable release with official Blackwell (sm_120)
# support, needed for RTX 50-series / H200-class GPUs. Installed from the
# cu128 wheel index via the Dockerfile (not PyPI default) so the right
# CUDA kernels are baked in.
torch==2.7.0 ; sys_platform != "win32"
torchvision==0.22.0 ; sys_platform != "win32"
# Upstream IMPLabUniPr/swin2-mose runtime deps (inference only; training
# deps like pytorch-lightning, hydra-core, tensorboard, matplotlib are
# intentionally excluded).
timm==1.0.11
einops==0.8.0
easydict==1.13
kornia==0.7.3
prometheus-client==0.21.0
redis==5.1.1
