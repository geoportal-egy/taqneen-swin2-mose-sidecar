fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
numpy==1.26.4
rasterio==1.3.10
Pillow==10.4.0
PyYAML==6.0.2
# torch pulled via the NVIDIA CUDA 12.x wheel at build time:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.4.1
torch==2.4.1 ; sys_platform != "win32"
# Upstream IMPLabUniPr/swin2-mose runtime deps (inference only; training
# deps like pytorch-lightning, hydra-core, tensorboard, matplotlib are
# intentionally excluded).
timm==1.0.11
einops==0.8.0
easydict==1.13
kornia==0.7.3
prometheus-client==0.21.0
redis==5.1.1
