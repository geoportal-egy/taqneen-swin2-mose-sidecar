fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
numpy==1.26.4
rasterio==1.3.10
Pillow==10.4.0
# torch pulled via constraints; use the NVIDIA CUDA 12.x wheel at build time:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.4.1
torch==2.4.1 ; sys_platform != "win32"
# TODO(phase1): add timm, einops, and the Swin2-MoSE project dependencies
# once we vendor the upstream repo.
prometheus-client==0.21.0
redis==5.1.1
